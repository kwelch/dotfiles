#!/usr/bin/env bash

ln -is $PWD/.bash_profile ~/.bash_profile
ln -is $PWD/.gitignore_global ~/.gitignore_global
ln -is $PWD/.gitconfig ~/.gitconfig
ln -is $PWD/.npmrc ~/.npmrc
ln -is $PWD/.nvmrc ~/.nvmrc
